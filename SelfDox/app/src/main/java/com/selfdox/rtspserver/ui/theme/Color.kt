package com.selfdox.rtspserver.ui.theme

import androidx.compose.ui.graphics.Color

// DECISION: Dark theme only per D14 spec — Duolingo-inspired palette

// Background
val Background = Color(0xFF0D0D0D)
val Surface = Color(0xFF1A1A1A)
val SurfaceVariant = Color(0xFF252525)

// Primary — Indigo accent
val Primary = Color(0xFF6C63FF)
val PrimaryVariant = Color(0xFF5A52E0)
val OnPrimary = Color(0xFFFFFFFF)

// Secondary — Teal (live indicator)
val Secondary = Color(0xFF00D9A3)
val SecondaryVariant = Color(0xFF00B88A)
val OnSecondary = Color(0xFF0D0D0D)

// Error/Stop — Red
val Error = Color(0xFFFF4757)
val OnError = Color(0xFFFFFFFF)

// Text
val TextPrimary = Color(0xFFFFFFFF)
val TextSecondary = Color(0xFFB0B0B0)
val TextTertiary = Color(0xFF707070)

// Status colors
val LiveGreen = Color(0xFF00D9A3)
val IdleGrey = Color(0xFF707070)
val StopRed = Color(0xFFFF4757)

// Card colors
val CardBackground = Color(0xFF1A1A1A)
val CardBorder = Color(0xFF2A2A2A)
